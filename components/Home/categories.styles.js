import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	categoriesContainer: {
    marginTop: 10,
  },
	categoryItem: {
    paddingVertical: 15,
		marginHorizontal: 20,
    borderBottomWidth: 1,
  },
  categoryText: {
		fontFamily: "semibold",
    fontSize: 18,
  },
})

export default styles