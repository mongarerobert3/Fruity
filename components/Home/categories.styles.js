import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	categoryItem: {
    //borderBottomWidth: 1,
  },
  categoryText: {
		fontFamily: "semibold",
    fontSize: 18,
    marginLeft: 25,
    marginRight: -10,
    paddingVertical: 15,
  },
})

export default styles